{
  "name": "FreeGLUT",

  "build_types": ["debug", "release"],
  "bin_types": ["shared_lib", "static_lib"],

  "cmake": {
    "run_dir": "extern/libs/freeglut-3.0.0",
    "out_dir": "_build/{PLATFORM}/{BUILD_TYPE}/extrn/freeglut",
    "debug": {
      "defs": [
        {"CMAKE_BUILD_TYPE": "Debug"}
      ],
      "env": []
    },

    "release": {
      "defs": [
        {"CMAKE_BUILD_TYPE": "Release"}
      ],
      "env": []
    },

    "common": {
      "defs": [],
      "env": []
    }
  },

  "build": {
    "run_dir": "libs/core/{PLATFORM}/{BUILD_TYPE}"
  },

  "cleanUp": {

  },

  "install": {
  }
}